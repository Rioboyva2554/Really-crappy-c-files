#include <stdio.h>
#include <unistd.h>

int main() {
  int onemil;
  int seconds;
  while (main) {
      onemil++;
      printf("%d\n", onemil);
      if (onemil==1000000) {
          return 0;
    }
}

}
